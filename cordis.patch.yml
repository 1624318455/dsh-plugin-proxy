# @memef1f1y/dsh-plugin-proxy bundle patch.
# Mounts the host entry with schema defaults (proxy off). Switching happens
# at runtime through the `dsh-proxy:` section of $DSH_HOME/settings.yaml.

- insert:
    - id: dsh-proxy
      name: '@memef1f1y/dsh-plugin-proxy'
